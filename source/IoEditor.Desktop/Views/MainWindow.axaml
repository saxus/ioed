<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="using:IoEditor.Desktop.Converters"
        xmlns:ae="using:AvaloniaEdit"
        xmlns:views="using:IoEditor.Desktop.Views"
        xmlns:vm="using:IoEditor.Desktop.ViewModels"
        x:Class="IoEditor.Desktop.Views.MainWindow"
        x:DataType="vm:MainViewModel"
        Title="{Binding WindowTitle}"
        Width="900" Height="600"
        WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <conv:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter" />
    <conv:InstructionSegmentEqualityToBrushConverter x:Key="EqualityToBrushConverter" />
    <conv:XDocumentToTextDocumentConverter x:Key="XDocumentToTextDocumentConverter" />
    <conv:BoolToMaxHeightConverter x:Key="BoolToMaxHeightConverter" ExpandedHeight="600" />
    <conv:XElementToStringConverter x:Key="XElementToStringConverter" />
    <views:IndexedStepDataTemplate x:Key="InstructionStepTmpl" />
  </Window.Resources>

  <Grid RowDefinitions="Auto,*,Auto">
    <Menu Grid.Row="0">
      <MenuItem Header="File">
        <MenuItem Header="_Open" Command="{Binding OpenFilesCommand}" HotKey="Ctrl+O" />
        <MenuItem Header="_Save" Command="{Binding SaveFileCommand}" HotKey="Ctrl+S" />
        <MenuItem Header="Sa_ve as…" Command="{Binding SaveAsCommand}" HotKey="Ctrl+Shift+S" />
        <Separator />
        <MenuItem Header="E_xit" Command="{Binding ExitCommand}" />
      </MenuItem>
      <MenuItem Header="Merge">
        <MenuItem Header="_Refresh" Command="{Binding RefreshMergeCommand}" HotKey="F5" />
      </MenuItem>
    </Menu>

    <TabControl Grid.Row="1">
      <TabItem Header="Segments">
        <ListBox ItemsSource="{Binding MergeSegments}">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <StackPanel>
                <CheckBox IsChecked="{Binding IsExpanded}">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Foreground="White" Text="#" />
                    <TextBlock Foreground="White" Text="{Binding SegmentIndex}" FontWeight="Bold" />
                    <TextBlock Margin="6,0,0,0" Text="{Binding Equality}" Foreground="{Binding Equality, Converter={StaticResource EqualityToBrushConverter}}" />
                    <TextBlock Margin="6,0,0,0" Foreground="White" Text="{Binding SegmentName}" />
                    <TextBlock Margin="6,0,0,0" Foreground="Lime" Text="{Binding ReferenceSegment.Steps.Count, StringFormat='({0})'}" />
                  </StackPanel>
                </CheckBox>
                <ScrollViewer MaxHeight="{Binding IsExpanded, Converter={StaticResource BoolToMaxHeightConverter}}"
                              VerticalScrollBarVisibility="Auto">
                  <Grid Margin="40,6,0,6" ColumnDefinitions="*,*,*">
                    <ItemsControl Grid.Column="0"
                                  ItemsSource="{Binding ReferenceSegment.Steps}"
                                  ItemTemplate="{StaticResource InstructionStepTmpl}" />
                    <ItemsControl Grid.Column="1"
                                  ItemsSource="{Binding TargetSegment.Steps}"
                                  ItemTemplate="{StaticResource InstructionStepTmpl}" />
                    <ScrollViewer Grid.Column="2"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto">
                      <TextBox IsReadOnly="True"
                               MinHeight="100"
                               TextWrapping="Wrap"
                               AcceptsReturn="True"
                               Text="{Binding Differences}" />
                    </ScrollViewer>
                  </Grid>
                </ScrollViewer>
              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </TabItem>

      <TabItem Header="Reference Xml">
        <ae:TextEditor Name="ReferenceXmlViewer"
                       Loaded="XmlEditor_Loaded"
                       Background="{DynamicResource IoEditorXmlEditorBackground}"
                       Foreground="{DynamicResource IoEditorXmlEditorForeground}"
                       HorizontalScrollBarVisibility="Auto"
                       VerticalScrollBarVisibility="Auto"
                       IsReadOnly="True"
                       FontFamily="Consolas, Menlo, Courier New, monospace"
                       Document="{Binding Project.Reference.Instruction.Document, Converter={StaticResource XDocumentToTextDocumentConverter}}" />
      </TabItem>

      <TabItem Header="Target Xml">
        <ae:TextEditor Name="TargetXmlViewer"
                       Loaded="XmlEditor_Loaded"
                       Background="{DynamicResource IoEditorXmlEditorBackground}"
                       Foreground="{DynamicResource IoEditorXmlEditorForeground}"
                       HorizontalScrollBarVisibility="Auto"
                       VerticalScrollBarVisibility="Auto"
                       IsReadOnly="True"
                       FontFamily="Consolas, Menlo, Courier New, monospace"
                       Document="{Binding Project.Target.Instruction.Document, Converter={StaticResource XDocumentToTextDocumentConverter}}" />
      </TabItem>

      <TabItem Header="Generated Xml">
        <ae:TextEditor Name="GeneratedXmlViewer"
                       Loaded="XmlEditor_Loaded"
                       Background="{DynamicResource IoEditorXmlEditorBackground}"
                       Foreground="{DynamicResource IoEditorXmlEditorForeground}"
                       HorizontalScrollBarVisibility="Auto"
                       VerticalScrollBarVisibility="Auto"
                       IsReadOnly="True"
                       FontFamily="Consolas, Menlo, Courier New, monospace"
                       Document="{Binding Project.MergedInstruction, Converter={StaticResource XDocumentToTextDocumentConverter}}" />
      </TabItem>

      <TabItem Header="Step dictionary">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <ItemsControl ItemsSource="{Binding StepDictionaryRows}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <StackPanel Margin="0,6,0,0">
                  <TextBlock Foreground="White">
                    <Run Text="#" />
                    <Run Text="{Binding SerializedStepIndex}" Foreground="Wheat" FontWeight="Bold" />
                    <Run Text=" " />
                    <Run Text="{Binding IsCallout, StringFormat='IsCallout: {0}'}" />
                  </TextBlock>
                  <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <TextBox IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas, Menlo, Courier New, monospace"
                             Text="{Binding Element, Converter={StaticResource XElementToStringConverter}}" />
                  </ScrollViewer>
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </TabItem>
    </TabControl>

    <Border Grid.Row="2" Padding="8,4">
      <TextBlock Text="Ready." />
    </Border>
  </Grid>
</Window>
